# users 폴더 직접 접근 완전 차단
# 모든 파일은 api.php를 통해서만 다운로드 가능

# Apache 2.4+
<IfModule mod_authz_core.c>
    Require all denied
</IfModule>

# Apache 2.2
<IfModule !mod_authz_core.c>
    Order deny,allow
    Deny from all
</IfModule>

# 만약 위 설정이 작동하지 않을 경우 (일부 서버)
<IfModule mod_rewrite.c>
    RewriteEngine On
    RewriteRule .* - [F,L]
</IfModule>
