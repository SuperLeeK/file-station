services:
  filestation:
    build: .
    container_name: filestation
    restart: always
    ports:
      - "8080:80"
    volumes:
      - ./data:/var/www/html/data
      - ./users:/var/www/html/users
      - ./shared:/var/www/html/shared
    environment:
      - TZ=Asia/Seoul 512M
